# @creator-cfo/storage

This package defines the local-first persistence contract for Creator CFO: SQLite tables, migration SQL, file-vault collections, and path rules. Keep it framework-agnostic so the mobile app can execute the contract while tests verify the intent.

