{
  "name": "@creator-cfo/storage",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --emitDeclarationOnly false",
    "format": "prettier --check 'src/**/*.{ts,tsx}' 'tests/**/*.ts'",
    "lint": "eslint src tests --ext .ts,.tsx",
    "test": "vitest run tests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}

